runner:
  logger:
    leve: InfoLevel # DebugLevel, InfoLevel, WarnLevel, ErrorLevel, FatalLevel, PanicLevel
    encoding: console # console, json
    output_paths:
      - stdout
    error_output_paths:
      - stderr